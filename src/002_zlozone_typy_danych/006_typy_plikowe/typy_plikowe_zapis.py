# Otwarcie pliku do zapisu (lub stworzenie nowego pliku)
with open('dane.txt', 'w') as file:
    # Zapisanie tekstu do pliku
    file.write('To jest przykładowy tekst, który zostanie zapisany do pliku.')
